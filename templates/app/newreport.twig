{% extends "app/appbase.twig" %}

{% block title %}Eintragen{% endblock %}

{% block content %}
  <div class="wrapper narrow">
    <div>
      <h1>Protokoll anlegen</h1>
      <ul class="selectionlist">
      {% for et in examTypes %}
      <li>
        <a href="newreport/{{et.id}}" class="main-text">{{et.name}}</a>
      </li>
      {% endfor %}
      <li>
        <a href="newreport/promotion" class="main-text">Arbeitsgruppenbericht / Promotion</a>
      </li>
    </ul>
    </div>
  </div>
{% endblock %}