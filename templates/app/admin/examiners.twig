{% extends "app/admin/adminreportbase.twig" %}

{% block admincontent %}
  <h1>Dozenten</h1>
  <div class="card mb">
    <h3>Suche</h3>
    <form method="get">
      <div class="inputlabel">
        <label for="examinersearchinput">Dozentensuche</label>
        <input type="search" name="searchExaminer" id="examinersearchinput" value="{{searchQuery}}" placeholder="Joachim Kirsch">
      </div>
      <div class="flex-lr-stack mtop16">
        <div>
          <input type="checkbox" name="newestFirst" id="cbNewestFirst" class="lbl" {% if newestFirst %}checked{% endif %}>
          <label class="check" for="cbNewestFirst">neueste zuerst</label>
        </div>
        <button type="submit" class="iconbtn"><i class="material-icons">search</i> suchen</button>
      </div>
    </form>
  </div>
  <ul class="striped">
    {% for examiner in examiners %}
      <li>
        <a href="examiner/{{examiner.id}}" class="main-text">{{examiner.name}}<small> - seit {{examiner.createdAtReadable}}</small></a>
      </li>
    {% endfor %}
  </ul>
  {% if pageCount > 1 %}
  Seiten:
    {% for p in 1..pageCount %}
      {% if loop.index == pageIndex %}
        <strong>{{p}}</strong>
      {% else %}
        <a href="?page={{p}}{% if newestFirst %}&newestFirst=on{% endif %}">{{p}}</a>
      {% endif %}
    {% endfor %}
  {% endif %}
{% endblock %}

{% block js %}
<script type="text/javascript">
  
</script>
{% endblock %}