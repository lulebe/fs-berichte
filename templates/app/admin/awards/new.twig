{% extends "app/appbase.twig" %}

{% block title %}PaLMe{% endblock %}
{% block head %}
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/easymde/dist/easymde.min.css">
<script src="https://cdn.jsdelivr.net/npm/easymde/dist/easymde.min.js"></script>
{% endblock %}

{% block content %}
<div class="wrapper narrow">
  <section>
    <h1>neue PaLMe anlegen</h1>
    <p>
      Wenn du einen neuen Preis anlegst, ist dieser zunächst "unveröffentlicht". Er ist dann nur über den Link erreichbar und es kann nicht abgestimmt werden.
      Sobald alle Daten eingegeben wurden, kann er dann auf "veröffentlicht" gesetzt werden.
    </p>
    <form method="post">
    <div class="card mb">
      <div class="inputlabel">
        <label>Titel</label>
        <input type="text" name="title" required placeholder="Wahl zum besten Holzwurmdompteur">
      </div><br>
      <div class="inputlabel">
        <label>Abstimmung möglich bis:</label>
        <input type="date" name="deadline" required value="{{deadlineDate}}" min="{{currentDate}}">
      </div><br>
      <input type="checkbox" class="lbl" name="shuffleCandidates" id="cbshuffle" value="true">
      <label class="check" for="cbshuffle">Kandidaten zufällig sortieren</label><br><br>
    </div>
    <div class="card">
      <div class="inputlabel">
        <label>Kurzbeschreibung (20-250 Zeichen)</label>
        <textarea name="description" class="short usertext" required minlength="20" placeholder="Das steht dann auf der Übersicht."></textarea>
      </div>
    </div>
    <div style="height: 64px">&nbsp;</div>
    <button type="submit" class="fullpagesubmit">speichern</button>
    </form>
  </section>
</div>
{% endblock %}