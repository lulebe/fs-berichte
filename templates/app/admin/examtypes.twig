{% extends "app/admin/adminreportbase.twig" %}

{% block admincontent %}
  <h1>Prüfungsarten</h1>
  <a href="examtype/new" class="btn iconbtn"><i class="material-icons">add</i>Neue Prüfungsart anlegen</a>
  <ul>
    {% for examType in examTypes %}
      <li>
        <a href="examtype/{{examType.id}}" class="main-text">{{examType.name}}<small> - seit {{examType.createdAtReadable}}</small></a>
      </li>
    {% endfor %}
  </ul>
{% endblock %}

{% block js %}
<script type="text/javascript">
  
</script>
{% endblock %}